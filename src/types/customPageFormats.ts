/** Format de page enregistré par l'utilisateur (dimensions en portrait). */
export interface CustomPageFormat {
  id: string;
  name: string;
  widthMm: number;
  heightMm: number;
  createdAt: string;
  updatedAt: string;
}
